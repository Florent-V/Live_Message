{{ form_start(form) }}
        {{ form_row(form.author, {
            label: 'Nom',
            attr: {
                placeholder: 'John Doe'
            },
            row_attr: {
                class: 'mb-3'
            }
        }) }}

    {{ form_row(form.content, {
        label: 'Message',
        attr: {
            placeholder: 'Je te souhaite une joyeuse retraite...'
        },
        row_attr: {
            class: 'mb-3'
        }
    }) }}
    <div id="charCounter">

    </div>
    <button class="btn btn-primary">
        <i class="fa-solid fa-paper-plane"></i>
        {{ button_label|default('Envoyer') }}
        <i class="fa-solid fa-paper-plane"></i>
    </button>
{{ form_end(form) }}
